@model IEnumerable<AspnetIdentitySample.Models.Pet>

@{
    ViewBag.Title = "Pet Index";
}

<h2>Pet Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Breed)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateOfBirth)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MicrochipNumber)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Breed)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateOfBirth)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MicrochipNumber)
        </td>
        <td>
            <a href="@Url.Action("Edit", "Pets", new { id = item.Id })" class="btn btn-primary btn-large">Edit</a> 
            <a href="@Url.Action("Details", "Pets", new { id = item.Id })" class="btn btn-primary btn-large">Details</a>
            <a href="@Url.Action("Delete", "Pets", new { id = item.Id })" class="btn btn-primary btn-large">Delete</a>
            <a href="@Url.Action("Rabies", "Pets", new { id = item.Id })" class="btn btn-primary btn-large">Rabies Vax</a>
            <a href="@Url.Action("Bloodtest", "Pets", new { id = item.Id })" class="btn btn-primary btn-large">Bloodtest</a>
            <a href="@Url.Action("Upload", "Pets", new { id = item.Id })" c class="btn btn-primary btn-large">Upload</a>
            <a href="@Url.Action("PetCheck", "Pets", new { id = item.Id })" class="btn btn-primary btn-large">PetCheck</a>
            <a href="@Url.Action("Cert", "Pets", new { id = item.Id })" c class="btn btn-primary btn-large">Cert</a>
        </td>
    </tr>
}

</table>
